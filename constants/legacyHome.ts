/**
 * Framer-exported home page served as static HTML from `public/legacy/index.html`.
 * Using this URL with `LegacyInlinePage` is the only way to get a pixel-identical clone
 * (layout, breakpoints, Framer animations, and runtime).
 */
export const LEGACY_HOME_HTML_URL = '/legacy/index.html' as const
