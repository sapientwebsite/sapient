import HomePage from '~/components/home/HomePage'

/** React rebuild of home (WIP). Production home `/` uses legacy export for exact parity. */
export default function HomeReactRoute() {
  return <HomePage />
}
